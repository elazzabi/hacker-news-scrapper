{
  "scripts": {
    "compile": "babel --presets es2015 -d lib/ src/",
    "test": "npm run compile && ava --verbose"
  },
  "dependencies": {
    "bluebird": "^3.5.0",
    "x-ray": "^2.3.2"
  },
  "devDependencies": {
    "ava": "^0.19.1",
    "babel-cli": "^6.24.1",
    "babel-preset-es2015": "^6.24.1"
  },
  "babel": {
    "presets": [
      "es2015"
    ]
  },
  "ava": {
    "files": [
      "test/*.js"
    ]
  }
}
